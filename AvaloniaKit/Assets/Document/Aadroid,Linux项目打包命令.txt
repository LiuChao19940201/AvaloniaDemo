/*

     # 先切到 Android 项目目录
     cd AvaloniaKit.Android

     # 清理项目
     dotnet clean AvaloniaKit.Android.csproj -c Release

     # 构建并打包 Android 项目（以 net10.0-android 为例）
     dotnet build AvaloniaKit.Android.csproj -c Release -f:net10.0-android -r android-arm64 -p:AndroidPackageFormat=apk --self-contained false

 */


  /*

    # 打包发布到 Linux 系统
    dotnet publish -c Release  -f:net10.0  -r linux-x64 --self-contained true  -o ./bin/Release/LinuxOutput

    # Linux系统中下载.net的运行时
    sudo apt-get update
    sudo apt-get install dotnet-runtime-10.0

    # 运行在Linux系统中
    chmod +x ./AvaloniaKit.Desktop
    ./AvaloniaKit.Desktop

  */